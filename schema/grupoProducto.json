{
  "$id": "http://example.com/example.json",
  "type": "object",
  "restricted": [],
  "key": "grupoProducto",
  "namespace": "inventario",
  "title": "Grupo Producto",
  "properties": {
    "id": {
      "$id": "/properties/id",
      "type": "integer",
      "title": "Id",
      "default": 0,
      "width": 50,
      "filter": "eq",
      "editable": true,
      "examples": [1]
    },
    "name": {
      "$id": "/properties/name",
      "type": "string",
      "title": "Nombre",
      "default": "",
      "filter": "LIKE",
      "width": 200,
      "editable": true,
      "filterable": true
    }
  },
  "actions": ["Crear", "Editar"],
  "required": ["name"],
  "layout": [{ "title": "Información", "fields": ["name"] }],
  "table": {
    "fields": ["id", "name"]
  }
}
